#pragma once

// ================================================================
// GameState.h の役割
// ---------------------------------------------------------------
// ・ゲーム全体の画面遷移状態を列挙する共通定義。
// ・main/scene/update各所で同じ状態値を参照するため独立ヘッダ化している。
// ================================================================

// ゲームの状態を全ファイルで共有するために独立させる
enum class GameState {
    START,
    SELECT,
    PLAYING,
    DEAD_SCREEN,
    DEADING_SCREEN,
    Deback,
    EDITOR,
    PAUSE
};